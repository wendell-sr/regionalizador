# Por padrão, o frontend usa o rewrite do Next.js (/api/backend/* → http://localhost:8000/*).
# Defina esta variável APENAS se quiser chamar o backend direto (sem proxy), ex.:
#   NEXT_PUBLIC_API_URL=http://localhost:8000
# Em produção, deixe vazio para usar o mesmo domínio.
# NEXT_PUBLIC_API_URL=
